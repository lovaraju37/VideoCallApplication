spring.application.name=call

# Server
server.port=8080

# MySQL Database (persistent)
# Make sure the 'videocall' database exists and credentials are valid
spring.datasource.url=jdbc:mysql://localhost:3306/videocall?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Lovaraju123

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (not used with MySQL)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# JWT
# NOTE: Replace with your own Base64-encoded 256-bit secret in production
app.jwt.secret=dGhpc2lzYWRldmVsb3BtZW50LXNlY3JldC1rZXktdXNlLWEtYmV0dGVyLW9uZQ==
app.jwt.ttl-ms=86400000
